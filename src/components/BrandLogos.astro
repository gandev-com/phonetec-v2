---
import {
  siApple,
  siGoogle,
  siHuawei,
  siLg,
  siMotorola,
  siOneplus,
  siSamsung,
  siXiaomi,
  type SimpleIcon,
} from 'simple-icons';

type Brand = {
  name: string;
  icon?: SimpleIcon;
};

const brands: Brand[] = [
  { name: 'Apple', icon: siApple },
  { name: 'Google', icon: siGoogle },
  { name: 'Huawei', icon: siHuawei },
  { name: 'LG', icon: siLg },
  { name: 'Motorola', icon: siMotorola },
  { name: 'OnePlus', icon: siOneplus },
  { name: 'Samsung', icon: siSamsung },
  { name: 'Xiaomi', icon: siXiaomi },
];
---

<div class="grid grid-cols-2 gap-4 sm:grid-cols-4 lg:grid-cols-8">
  {brands.map((brand) => (
    <div class="rounded-2xl border border-white/10 bg-white/5 px-4 py-5 text-center transition-transform duration-300 hover:-translate-y-1 hover:border-phonetec/40">
      <div class="flex min-h-12 items-center justify-center">
        {brand.icon ? (
          <svg
            viewBox="0 0 24 24"
            class="h-8 w-8"
            fill={`#${brand.icon.hex}`}
            aria-hidden="true"
            set:html={`<path d="${brand.icon.path}" />`}
          ></svg>
        ) : (
          <span class="text-base font-extrabold tracking-wide text-slate-300">{brand.name}</span>
        )}
      </div>
      <p class="mt-3 text-sm font-semibold text-slate-400">{brand.name}</p>
    </div>
  ))}
</div>