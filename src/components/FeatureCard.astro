---
interface Props {
  title: string;
  description: string;
  eyebrow?: string;
}

const { title, description, eyebrow } = Astro.props;
---

<article class="rounded-3xl border border-white/10 bg-white/5 p-7 shadow-md shadow-black/20 backdrop-blur transition-transform duration-300 hover:-translate-y-1" data-reveal>
  {eyebrow && <p class="text-sm font-semibold uppercase tracking-[0.2em] text-phonetec">{eyebrow}</p>}
  <h3 class="mt-3 text-2xl font-bold text-white">{title}</h3>
  <p class="mt-4 text-base leading-8 text-slate-400">{description}</p>
</article>